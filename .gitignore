# engine sources symlinked in for the Beagle build (Fram is the source of truth)
src/fram

# tenant registry holds token hashes + topology — keep it out of git
deploy/gateway/tenants.edn

# NOTE: the compiled Clojure in out/ IS committed (Lodestar runs on bare
# babashka, no Beagle needed at runtime — mirrors Fram). Rebuild with ./build.sh
# and commit the result when the .bclj sources change.

# npm-generated lockfile in bun projects (bun.lock is the source of truth)
sdk/package-lock.json

# Nix/direnv + project-local Elixir tooling (web/ dev shell)
.direnv/
.mix/
.hex/
erl_crash.dump
