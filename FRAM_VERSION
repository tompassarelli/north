# The Fram engine commit that Lodestar's committed out/ is built and tested
# against. Fram's main moves independently and Lodestar links its library API
# (fram.kernel/fold/import/export/clock/json/rt), so we pin instead of tracking
# main. Bump deliberately: check out the new Fram, rebuild (./build.sh), re-run
# the tests, and update this SHA. CI and the Dockerfile both read this pin.
e78badabb43aa8ce1f507b8a8d74b86737cd34de
