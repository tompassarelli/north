# Status + Next Actions — 2026-06-29

## Where things stand

**The lodestar-native coordination redesign is DONE and LIVE.** 10/10 threads landed by
parallel SDK agents over the graph, activated via a `:7977` restart (the new engine re-folded
the 2.8 MB production log cleanly; `doctor` UP + serving + matches-log).

Built + live in the fram engine + lodestar:
- **cardinality-as-a-claim** (sole authority; kernel list demoted to a boot seed)
- **coexist-elect** (rival claims coexist; deterministic read-time election; no writer blocks)
- **per-(s,p) OCC write verbs** (`append!`/`put!`/`swap!`; engine `:base` optional; global-version CAS ritual deleted)
- **views-as-claims** (per-branch isolation; `select!`/`commit-on-view!`)
- **snapshot / compaction / as-of** (materialized head + tail-fold + incremental boot)
- **incremental-aggregate primitive** (budget = Σ(@run cost), quorum = count-distinct — one fold, two reducers; `:bump` counter + `@swarm-slot` semaphore deleted)
- **first-class retraction** (as-of + `withdrawn_*`, add-wins/remove-wins per view)
- **command-as-claims** (content-addressed `@cmd` claims + Datalog reactor; `{:op :args}` blob, duplicated parser, reply-schema sidecar, reactor lease, 150ms sleep all deleted)
- **concern-over-code** (footprint = code-graph blast-joins over node identity, rename-stable; degrades to path-strings for non-flipped repos)
- **predicate registry** (`@pred:*` + `pred lint`), **shared `cli/coord.clj` substrate**, **P0 presence port-partition fix** (`:7978`→`:7977`)

Also this session: real-time interrupt (listener wake / PostToolUse heartbeat / SDK `streamInput`),
auto-connect-on-spawn (SessionStart/SubagentStart hooks), terminology fix (fact→claim), gitleaks on PATH.

**The dogfood is proven** — your #1 open thread ("validate auto-coordination on real work") is closed:
agents drove real engine + CLI implementation in parallel, coordinated via concerns (budget+quorum
*shared a primitive* off a surfaced overlap), and the later ones self-closed end-to-end.

## Strongly-suggested next actions (ranked)

**1. Behaviorally validate the LIVE engine (do first).** We verified the re-fold + the test suites,
but those run against test fixtures. Now that `:7977` runs the new code, prove the NEW *semantics*
actually work on the production daemon: a coexist-elect probe (two rival single writes coexist +
elect deterministically), a view-isolation check, a cardinality-claim enforcement check, an as-of
query. You just flipped a big switch — confirm it's not just "didn't crash."

**2. Fix the coordination backstop — escalate-not-guillotine (`019f10ae`, in the epic).** The
`maxTurns` cap is a known band-aid that guillotined two agents mid-work this session. The real fix:
bound on SPEND (budget=Σ@run, already built) wired into the SDK spawn/dispatch path; detect *struggle*
by progress not turn-count; on a hard task CAPTURE context + ESCALATE (higher tier / fresh agent),
never kill. Calibrate the trigger off `@run` telemetry. This is the principled replacement for a
crude bound that fights the substrate.

**3. The v1 debts the dogfood agents flagged (quick, concrete):**
- `command`: scoped command push needs a fram-side `sub-match?` tweak (`--scoped` only matches `p="to"`).
- `concern-over-code`: O(delta) incremental `calls_defn` (today re-derives whole-corpus on a code edit, cached); cold flip-off/on node-id stability.
- `budget`: prune the dead `budget_spent` predicate in `pred-cli`; `budget_total` is now USD not tokens — set a dollar cap.

**4. Retire the vestigial `:7978` agent-coord daemon.** The port-fix moved presence onto `:7977`;
`:7978` no longer has readers. Confirm nothing writes it, then retire it (and `:7980` attention-coord if also unused).

**5. The 42-file reconcile (cosmetic).** Ingest the legit file-ahead claims or regenerate the `.md`
projection from the log; clears the `doctor` WARN to green. ~5 min.

**6. NORTH STAR — author the fram engine in Beagle (big, foundational).** The one place we could NOT
parallelize this session is editing the engine itself, because `cnf_coord.clj` is hand-written
Clojure, not code-as-claims. Flip the engine to Beagle and the engine becomes claim-native like
everything else — the ultimate dogfood, and it makes engine work parallelizable. Large undertaking;
the real end-game.

**7. The control-panel app (your recurring want).** "One command → see every knob, its state, its
impact + profiles." Still underspecified; the roster + concerns + the `@run` telemetry are the data
substrate it would render. Worth a spec pass when the engine work settles.
