---
name: webdev-distilled
description: >-
  Use when writing or reviewing web UI: HTML/CSS/JS, forms, layout,
  responsive/mobile behavior. Default to these cross-project house rules; keep
  framework- and repo-specific guidance in each repository.
---

# Web UI

Keep focusable `input`, `select`, and `textarea`, including active/focus states, at computed `font-size` ≥16px. Never use `user-scalable=no` or `maximum-scale=1`; they disable pinch zoom.

Details: `agents path webdev-reference`.
