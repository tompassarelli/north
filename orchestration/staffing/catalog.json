{
  "$schema": "./catalog.schema.json",
  "version": 2,
  "vocabulary": {
    "taskGrades": ["novice", "junior", "mid", "senior", "staff", "principal", "distinguished"],
    "semanticTiers": ["economy", "standard", "senior", "frontier"],
    "deliberations": ["low", "medium", "high", "xhigh", "max"],
    "topologies": ["worker", "orchestrator"],
    "postures": ["explore", "deliver", "preserve", "evaluate"],
    "capabilities": ["filesystem.read", "filesystem.search", "filesystem.write", "shell", "shell.readonly", "web", "coordination", "graph-authoring.fram"]
  },
  "defaults": {
    "taskGrade": "mid", "tier": "standard", "deliberation": "medium", "topology": "worker", "posture": "explore"
  },
  "presets": [
    {
      "name": "executor", "taskGrade": "novice", "tier": "economy", "deliberation": "low", "topology": "worker", "posture": "deliver",
      "capabilities": ["filesystem.read", "filesystem.search", "filesystem.write", "shell"],
      "tagline": "the specified change, applied exactly",
      "description": "Execute-shaped tasks — bounded, mechanical, fully specified: apply a patch, rename a symbol, add obvious tests, fix lint. Any judgment call → implementer; behavior crossing a seam → integrator."
    },
    {
      "name": "implementer", "taskGrade": "mid", "tier": "standard", "deliberation": "medium", "topology": "worker", "posture": "deliver",
      "capabilities": ["filesystem.read", "filesystem.search", "filesystem.write", "shell"],
      "tagline": "a working feature or fix inside existing patterns",
      "description": "Implement-shaped tasks — one feature or fix inside known patterns. Ambiguous cross-seam debugging → integrator; choosing a new shape → designer."
    },
    {
      "name": "integrator", "taskGrade": "senior", "tier": "senior", "deliberation": "high", "topology": "worker", "posture": "deliver",
      "capabilities": ["filesystem.read", "filesystem.search", "filesystem.write", "shell"],
      "tagline": "a working change across seams, with the map of what moved",
      "description": "Integrate-shaped tasks — changes across consequential behavioral seams, ambiguous debugging, refactors with behavior at stake. Choosing a new design shape rather than working within one → designer."
    },
    {
      "name": "designer", "taskGrade": "staff", "tier": "frontier", "deliberation": "xhigh", "topology": "worker", "posture": "explore",
      "capabilities": ["filesystem.read", "filesystem.search", "shell.readonly"],
      "tagline": "a decision with trade-offs, not code",
      "description": "Design-shaped tasks — choosing the shape of things: APIs, data models, decomposition, naming that commits the system; small decisions with large blast radius are design, not execute. Produces a DECISION with trade-offs, not code — read-only by design."
    },
    {
      "name": "director", "taskGrade": "staff", "tier": "frontier", "deliberation": "xhigh", "topology": "orchestrator", "posture": "deliver",
      "capabilities": ["filesystem.read", "filesystem.search", "shell.readonly", "web", "coordination"],
      "tagline": "independent work reconciled into one evidence-backed result",
      "description": "Director-shaped work — two or more genuinely independent pieces whose seams must be designed, staffed, and evidence-reconciled. Coordinates only: freshly admits each child, consumes evidence, independently spot-checks materially load-bearing seams, drives the aggregate end-to-end, and returns one result; never executes worker subtasks. Atomic or tightly coupled work → a worker role."
    },
    {
      "name": "team-lead", "taskGrade": "staff", "tier": "senior", "deliberation": "high", "topology": "orchestrator", "posture": "deliver",
      "capabilities": ["filesystem.read", "filesystem.search", "shell.readonly", "web", "coordination"],
      "tagline": "one workstream reconciled into a single evidence-backed result",
      "description": "Team-lead orchestration — first rung of the scope ladder: coordinate ONE workstream (a bounded tree of directly staffed children) to a single reconciled, evidence-backed result. Sees its own workstream in full and the program above only as constraints. Several workstreams → program; the whole board → portfolio."
    },
    {
      "name": "program", "taskGrade": "principal", "tier": "frontier", "deliberation": "xhigh", "topology": "orchestrator", "posture": "deliver",
      "capabilities": ["filesystem.read", "filesystem.search", "shell.readonly", "web", "coordination"],
      "tagline": "several workstreams reconciled into one program outcome, with the cross-workstream seam map",
      "description": "Program orchestration — middle rung of the scope ladder: coordinate SEVERAL workstreams toward one program goal; owns the seams between workstreams and their sequencing, never any lane's interior. One workstream → team-lead; the whole board → portfolio."
    },
    {
      "name": "portfolio", "taskGrade": "distinguished", "tier": "frontier", "deliberation": "xhigh", "topology": "orchestrator", "posture": "deliver",
      "capabilities": ["filesystem.read", "filesystem.search", "shell.readonly", "web", "coordination"],
      "tagline": "the board held coherent — programs reconciled into one portfolio outcome",
      "description": "Portfolio orchestration — top rung of the scope ladder: hold the whole board; coordinate programs, own cross-program seams and priorities, below only a human owner. Reasons in programs and their seams, never lane detail."
    },
    {
      "name": "scout", "taskGrade": "junior", "tier": "economy", "deliberation": "low", "topology": "worker", "posture": "explore",
      "capabilities": ["filesystem.read", "filesystem.search", "shell.readonly", "web"],
      "tagline": "gathered findings, with provenance",
      "description": "Scout work — locate, map, gather: where is X, what calls Y, sweep a codebase or the web for sources. Read-only, low-cost fan-out unit; spawn several in parallel. Gathers and reports without deep synthesis. Deep analysis or root-cause → analyst; cutting-edge inquiry → scientist."
    },
    {
      "name": "analyst", "taskGrade": "senior", "tier": "senior", "deliberation": "high", "topology": "worker", "posture": "explore",
      "capabilities": ["filesystem.read", "filesystem.search", "shell.readonly", "web"],
      "tagline": "understanding, grounded in real behavior",
      "description": "Analysis work — how a system actually works, why it behaves as it does, root-cause, or grounding a design against real behavior. Read-only, depth over breadth; falls back to explicitly static-only analysis, naming unobserved behavior, when no read-only execution surface exists. Produces UNDERSTANDING — not a disposition (→ reviewer), a decision (→ designer), or a change (→ integrator); mere location/gathering → scout. Fan out when independently traceable mechanisms warrant separate evidence trails; keep tightly coupled traces together regardless of subsystem count."
    },
    {
      "name": "reviewer", "taskGrade": "senior", "tier": "senior", "deliberation": "high", "topology": "worker", "posture": "evaluate",
      "capabilities": ["filesystem.read", "filesystem.search", "shell.readonly"],
      "tagline": "one supplied artifact reviewed across multiple criteria, with prioritized findings and a disposition",
      "description": "Artifact review — one supplied artifact or change in; evidence-backed prioritized findings plus accept, changes-required, or cannot-assess out. Read-only; never fixes or redesigns. One claim → verifier; ranking alternatives → judge; mechanism explanation → analyst."
    },
    {
      "name": "verifier", "taskGrade": "senior", "tier": "senior", "deliberation": "high", "topology": "worker", "posture": "evaluate",
      "capabilities": ["filesystem.read", "filesystem.search", "shell.readonly"],
      "tagline": "one claim in, one adversarial verdict out",
      "description": "Adversarial verification of one specific claim at any leverage — \"is this bug real\", \"does this fix actually hold\", \"try to refute this\". The standard fan-out unit for verify stages. Ranking alternatives → judge; multi-criterion review of one artifact → reviewer."
    },
    {
      "name": "judge", "taskGrade": "staff", "tier": "frontier", "deliberation": "xhigh", "topology": "worker", "posture": "evaluate",
      "capabilities": ["filesystem.read", "filesystem.search", "shell.readonly"],
      "tagline": "competing alternatives in, a ranked decision out",
      "description": "Rubric-backed ranking of two or more supplied alternatives — scores, a winner, and what to graft from runners-up. Does not verify a single claim (→ verifier) or invent the candidate set (→ designer). Read-only."
    },
    {
      "name": "scientist", "taskGrade": "staff", "tier": "frontier", "deliberation": "xhigh", "topology": "worker", "posture": "explore",
      "capabilities": ["filesystem.read", "filesystem.search", "shell.readonly", "web"],
      "tagline": "hypotheses, discriminating experiments, and evidence-bounded analysis",
      "description": "Research-grade inquiry — open solution class, novel method, answer genuinely unknown. Frames competing hypotheses and discriminating experiments; invokes existing non-mutating probes only, never authors code or apparatus (hand that to an authoring role). Source gathering → scout; ordinary root-cause → analyst. Research is a function, not a task grade."
    }
  ],
  "aliases": []
}
