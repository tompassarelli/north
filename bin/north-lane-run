#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd -P)"
FRAM_HOME="${FRAM_HOME:-$HOME/code/fram/main}"
FRAM_OUT="${FRAM_OUT:-$FRAM_HOME/out}"
BB="${NORTH_BB:-bb}"
exec "$BB" -cp "$FRAM_OUT" "$ROOT/cli/lane-run.clj" "$@"
