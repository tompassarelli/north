# North R-1 apparatus — pre-production, test-only

North-side companion to Reification R-1 (thread `019f79eb-f6a4`), against the B2
contract (`019f79eb-e8e0`, §4 attribution + cross-runtime identity). **Test-only**:
touches no production/runtime/package source. This top-level `apparatus_r1/` dir
is referenced by no package install phase (North's flake copies only named paths),
so it is excluded from every North closure. No daemon, no port, no live North/FRAM
state.

## Cells

1. **Cross-repo / cross-runtime identity** (`kev.mjs`, `corpus/`): North's JS
   `K_ev` oracle — an independent reimplementation of the raw-byte identity law —
   must reproduce the **golden vector** (`corpus/golden.*.tsv`, generated by
   fram's bb/JVM oracle) **byte-for-byte**. This closes the bb == JVM == JS loop
   across repositories, and re-exhibits the S1 divergence (B2 keeps the
   byte-identical append; B-prime suppresses it).

2. **§4 attribution reference model** (`attribution.mjs`): the four-envelope
   precedence + legacy `:frame` law + R-compat receipt matrix, pinned to
   **current observed North source**:
   - fact `:by` order `NORTH_AGENT_ID > AGENT_ID > "user"` — `src/north/main.bclj:834-842`;
   - legacy real-edit classifier `∈ {coord,agent,cli}` — `src/north/staleness.bclj:36-38`;
   - mcp-coordinator order `explicit > AGENT_ID > NORTH_AGENT_ID` (a **distinct**
     surface, unchanged in both models) — `bin/north-mcp:276-285`.
   RED against B-prime's revoked AGENT_ID-first order; GREEN against B2.

A negative-control cell proves the JS harness fails loud (nonzero) on a wrong
verdict.

## Run

```sh
apparatus_r1/run.sh      # identity golden diff + §4 model + negative control; exit 0 = green
```

## Limitations

- The attribution model is an independent oracle of the §4 laws, grounded in the
  cited source seams (read this session); it does not execute the North daemon or
  web gateway.
- The golden `K_ev` vector is produced by fram's oracle and committed here as the
  cross-repo agreement receipt; regenerate with fram's
  `tests/apparatus_r1/identity/kev.clj` over `corpus/`.
